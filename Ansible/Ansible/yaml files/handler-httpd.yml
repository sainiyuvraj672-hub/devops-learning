---
- name: handler the httpd
  hosts: db
  become: true

  tasks:
    - name: copy website file
      copy:
        content: "hello from ansible handler!! 😤🔥"
        dest: /var/www/html/index.html
      notify: restart apache

  handlers:
    - name: restart apache
      service:
        name: httpd
        state: restarted
